# 性能基线 2026-09-16 21:29

## 环境

| 项 | 值 |
|---|---|
| HEAD | `096dd85`（分支 `codex/minor-fixes`，工作区干净） |
| 与 main 差异 | 55 文件，+3284 / −429（Rust 18 文件 +2128/−212；前端 31 文件 +762/−119） |
| 工具链 | node v24.21.0 / pnpm 11.9.0 / cargo、rustc 1.96.0 |
| 系统 | Windows 11 Pro for Workstations 10.0.26200 |
| 产物 | `pnpm tauri build` release，exe 构建于 2026-09-16 21:18，版本 0.17.4 |
| 命令 | `pwsh -File scripts/perf-probe.ps1 -Exe <target/release/cgswitch.exe> -Runs 3 -SoakSeconds 600` |

构建备注：tauri build 末尾因缺 `TAURI_SIGNING_PRIVATE_KEY` 更新器签名失败（exit 1），仅影响 `.sig` 签名文件，exe 与 NSIS/MSI 安装包均为本次构建正常产物。

## 探针结果

### 冷启动（App 全关 → 主窗口可见）

| Run | Visible_ms | Responsive_ms |
|---|---|---|
| 1 | 760 | 1367（首次样本，含 kill 后首启抖动） |
| 2 | 707 | 734 |
| 3 | 744 | 767 |

**中位数 744 ms（区间 707–760）**；响应就绪中位数约 767 ms。

### 托盘恢复（关窗 → 再点图标 → 窗口可见）

| Run | HiddenBefore | Restore_ms |
|---|---|---|
| 1 | True | 320 |
| 2 | True | 288 |
| 3 | True | 316 |

**中位数 316 ms（区间 288–320）**。

### 体积

- exe：9.0 MB
- 安装包（最新）：`CGswitch_0.17.4_x64_en-US.msi` 4.7 MB（同批 NSIS `CGswitch_0.17.4_x64-setup.exe` 亦已产出）

### 常驻资源（600 秒托盘挂机采样，30 秒间隔）

- 私有内存 43.0 → 43.8 MB（中途波动区间 41.7–45.2，无单调增长）
- 句柄 826 → 819（不增反降）
- 空闲 CPU：首个 30 秒 2.08%（启动余热），之后全部 ≈ 0.00–0.09%

**无泄漏信号，空闲开销可忽略。**

## 支撑层

### 前端 bundle 对比（HEAD vs main@`225b3fb`，同为 vite 生产构建）

| 产物 | main | HEAD | Δ |
|---|---|---|---|
| index JS | 1084.5 KB | 1127.8 KB（gzip 352.5 KB） | +43.3 KB |
| react-markdown JS | 113.2 KB | 115.9 KB | +2.7 KB |
| index CSS | 83.7 KB | 81.8 KB | −1.9 KB |
| js+css 合计 | 1281.4 KB | 1325.5 KB | **+44.1 KB（+3.4%）** |

### 启动路径静态核查

- `database.rs` 未改动 → 无新 migration，启动不多跑迁移。
- 新增 `mcp_probe.rs`（+1004）为 MCP 页按需探测，不在启动路径。
- 启动路径 Rust 新增仅 `lib.rs` +81 行。
- `process.rs` +504 / `connections.rs` +221 / `codex_oauth.rs` +125 均为运行时行为。

### Criterion Rust 基准

baseline 名：`perf-2026-09-16-2129`（首份，仅作未来对照锚点，本次无历史可比）

| 基准 | 中位耗时 |
|---|---|
| settings_read | 75.3 µs |
| settings_write | 1.086 ms |
| config_toml_parse_capture | 9.39 µs |
| config_toml_apply | 17.3 µs |
| config_toml_validate_valid | 6.66 µs |
| config_toml_validate_broken | 7.12 µs |
| config_toml_format | 20.4 µs |
| db_profiles_query_20 | 24.3 µs |

## 结论

1. **冷启动 744 ms 中位数**，本分支 HEAD 实测；这是仓库第一份基线，无历史数据可比。
2. **分支性能风险评估：无回归信号** —— 前端 bundle 仅 +44 KB（gzip 后约 +14 KB，解析耗毫秒级）；无新 DB migration；启动路径新增代码极少；MCP 探测为按需触发。静态证据与 HEAD 实测一致。
3. 冷启动 main vs HEAD 的 A/B 实测未做（用户选定只做体积对照），故「冷启动是否变慢」只能说**体积与启动路径证据均指向无影响**，不给出毫秒级归因。
4. 常驻 10 分钟：内存平稳、句柄下降、空闲 CPU ≈ 0。

## 尚未覆盖（每次报告重新点名）

- 维度 5 **首屏内容就绪**（列表可读非骨架屏）：需埋点或 WebView2 自动化，本次未覆盖。
- 维度 6 **页面切换**、维度 7 **等待类操作**：同上，未覆盖。
- 本轮未测安装后占盘（Installed_MB 为空，未指定安装目录）。

## 可比性说明

- 后续对比仅限：相同命令（perf-probe.ps1 同参数）、同机器、同 release 产物形态；Criterion 对比仅限同名 baseline。
- main 的 bundle 数据来自临时 worktree（`225b3fb`），非完整 release 探针，只用于体积对比，不用于时间对比。
